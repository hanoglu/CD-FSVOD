#!/usr/bin/env ipython
%cd /root/BHRL/VOTIMAGES/
# Edit BHRL/VOTIMAGES/vot_videos.txt to select which sequences to download
!awk -F ": " '{system("mkdir " $1 "; cd " $1 "; wget " $2 "; unzip * ;rm -f *.zip")}' vot_videos.txt
